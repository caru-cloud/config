server:
  port: 9000

eureka:
  instance:
    hostname: localhost
    appname: eureka-cluster
  client:
    registerWithEureka: false
    fetchRegistry: false
  server:
    evictionIntervalTimerInMs: 3000
